# Pokémon Birthday - 视觉设计系统规范 (Visual System Guide)

## 🎨 视觉底座 (Visual Foundation)
本系统采用“现代卡通 + 经典像素”的融合风格，旨在还原游戏童趣的同时提供现代化的 Web 交互体验。

### 1. 字体系统 (Typography)
- **主标题字体**: `Luckiest Guy` + `pokemon-font` 类。
- **像素特征**: 引入 `DotGothic16` 字体实现经典的 8-bit 像素感。
- **圆润感**: 使用 `Quicksand` 字体确保现代 UI 的亲和力。
- **中文字符**: 采用系统默认圆体/微软雅黑 UI 回退，确保高可读性。

### 2. 配色方案 (Color Palette)
- **精灵球红 (Poké Red)**: `#FF5A5F` (优化后的 WCAG 合规色)
- **皮卡丘黄 (Pika Yellow)**: `#FFCB05`
- **水系蓝 (Water Blue)**: `#2A75BB`
- **中性灰 (Neutral)**: `#1A1A1A`

## 🖼️ 核心插画组件 (Hero Illustrations)
在首页顶部精心安排了“趴卧姿态”的热门宝可梦：
- **皮卡丘 (Pikachu)**: 位于左侧，带有微弱的黄色外光晕。
- **小火龙 (Charmander)**: 位于右侧，带有微弱的橙色外光晕。
- **视觉平衡**: 两个角色通过 `motion.div` 实现了入场错落感，并在不同屏幕尺寸下自动缩放。

## 🌌 背景系统 (Background System)
- **卡通渐变**: 移除了纯白背景，改为基于官方色系的流体渐变。
- **高清宝可梦墙**: 
  - **模糊度**: 已降低，使角色细节清晰可见。
  - **对比度**: 提升至 `125%`，增强视觉冲击力。
  - **视差效果**: 背景墙随滚动产生深度偏移。

## 📱 响应式适配 (Responsive Design)
- **桌面端**: 标题采用 `text-8xl`，插画保持原始比例。
- **移动端**: 标题自动缩放至 `text-5xl`，插画尺寸减小并保持居中，确保不会遮挡表单。

---
*本规范文档由 Trae AI 生成，旨在指导后续视觉扩展与维护。*
